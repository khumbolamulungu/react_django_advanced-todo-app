# djangoadvanced-todo-app
this is an advanced django todo app that uses semantic-ui in the UI

#how to install the app
cd into the project's folder
then
pip install -r requirements.txt
then
python manage.py runserver
